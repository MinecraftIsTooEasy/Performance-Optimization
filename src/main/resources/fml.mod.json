{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "depends": {
    "fishmodloader": ">=3.0.0"
  },
  "environment": "server",
  "name": "${name}",
  "description": "",
  "authors": [
    "Inf1nlty"
  ],
  "entrypoints": {
    "main": [
      "com.inf1nlty.perfopt.OptimizeMod"
    ]
  },
  "mixins": [
    "perfopt.mixins.json"
  ]
}